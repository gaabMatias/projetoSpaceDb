const mongoose = require(`mongoose`)
const Schema = mongoose.Schema

const astroInfo = new Schema ({

})

const astronautSchema = new Schema ({

})

module.exports = {
    astronautSchema,
    astroInfo
}
console.log(astronautSchema)